<div class="content-card" data-id="{{id}}">
    <div class="card-image">
        <img src="{{image}}" alt="{{imageAlt}}">
    </div>
    <div class="card-info">
        <h3>{{title}}</h3>
        <div class="card-tags">
            {{#each tags}}
            <span>{{this}}</span>
            {{/each}}
        </div>
        <div class="card-rating">★ {{rating}}</div>
    </div>
</div>